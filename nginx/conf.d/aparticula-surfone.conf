####################################################
# MAGENTO
####################################################
server {
    server_name surfone-shop.dev;
    root /app/aparticula/surfone;

    access_log off;
    error_log off;

    include snippets/magento.conf;

    location ~ .php$ {
        ## Catch 404s that try_files miss
        if (!-e $request_filename) {
            rewrite / /index.php last;
        }
        expires        off; ## Do not cache dynamic content
        fastcgi_pass   php:9000;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        fastcgi_param  MAGE_RUN_CODE base;
        fastcgi_param  MAGE_RUN_TYPE website;
        fastcgi_param  MAGE_IS_DEVELOPER_MODE true;
        include        fastcgi_params;
    }

    location /media/catalog {
        return 302 https://surfone-leucate.com$request_uri;
    }
    location /media/wysiwyg {
        return 302 https://surfone-leucate.com$request_uri;
    }
}

server {
    server_name surfone-ecole.dev;
    root /app/aparticula/surfone;

    access_log off;
    error_log off;

    include snippets/magento.conf;

    location ~ .php$ {
        ## Catch 404s that try_files miss
        if (!-e $request_filename) {
            rewrite / /index.php last;
        }
        expires        off; ## Do not cache dynamic content
        fastcgi_pass   php:9000;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        fastcgi_param  MAGE_RUN_CODE barca;
        fastcgi_param  MAGE_RUN_TYPE website;
        include        fastcgi_params;
    }

    location /media/catalog {
        return 302 https://surfonebarcares.com$request_uri;
    }
    location /media/wysiwyg {
        return 302 https://surfonebarcares.com$request_uri;
    }
    location /media/webcam {
        return 302 https://surfonebarcares.com$request_uri;
    }
    location /media/spots {
        return 302 https://surfonebarcares.com$request_uri;
    }
}